int sign(int x)
    if x < 0
        return -1
    else
        if x == 0
            return 0
        else
            return 1

void main()
    print(sign(-1))
    print(sign(0))
    print(sign(2))
