int f(int x)
    return g(x) + h(x - 1)

int g(int x)
    if x <= 1
        return 1
    return x + g(x - 2)

int h(int x)
    if x <= 0
        return 0
    return x + h(x - 3)

void main()
    print(f(6))
