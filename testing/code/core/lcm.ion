int gcd(int a, int b)
    int t = 0
    while b != 0
        t = b
        b = a % b
        a = t
    return a

int lcm(int a, int b)
    int g = gcd(a, b)
    return (a / g) * b

void main()
    print("%i\n", lcm(21, 6))
    print("%i\n", lcm(8, 12))
