int steps(int n)
    int count = 0
    while n != 1
        if n % 2 == 0
            n = n / 2
        else
            n = 3 * n + 1
        count = count + 1
    return count

void main()
    print("%i\n", steps(27))
    print("%i\n", steps(7))
